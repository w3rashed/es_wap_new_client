import React from 'react';
import SectionContainer from '../SectionContainer';

const Hero = () => {
    return (
        <SectionContainer>
            <h1 className='bg-red-500'>Hero</h1>
        </SectionContainer>
    );
};

export default Hero;